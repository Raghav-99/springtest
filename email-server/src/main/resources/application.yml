spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: "update"
  datasource:
    url: "jdbc:mysql://localhost:3306/testdb"
    username: "root"
    password: ""
management:
  endpoints:
    web:
      exposure:
        include: "health,info,beans,caches"